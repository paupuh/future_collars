function power(base, exponent) {
  if (typeof base === "number" && typeof exponent === "number")
    console.log("Arguments are correct");
  return Math.pow(base, exponent);
}
console.log(power(2,3));

// Stwórz funkcję pow, która będzie wykonywała działanie potęgowania. Funkcja będzie przyjmowała dwa argumenty typu number.
// Pierwszym parametrem będzie podstawa potęgi, a drugim wykładnik. Funkcja powinna zwrócić wykonaną operację.

// Przykładowy input:

// Cyfry 2 i 2, cyfry 3 i 3

// Przykładowy output:

// 4, 27
